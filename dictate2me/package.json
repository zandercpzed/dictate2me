{
  "name": "obsidian-dictate2me",
  "version": "0.1.0",
  "description": "Plugin Obsidian para transcrição de voz 100% local.",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=cjs --platform=node --external:obsidian",
    "dev": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=cjs --platform=node --external:obsidian --watch"
  },
  "keywords": ["obsidian-plugin", "transcription", "voz", "local"],
  "author": "zandercpzed",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.2.2"
  }
}
